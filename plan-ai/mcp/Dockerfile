FROM ghcr.io/abhigyanpatwari/gitnexus:latest

# For customer analysis, we do NOT copy your own codebase into the image.
# Instead, Railway will deploy this empty server, and you MUST attach a 
# Railway Volume mounted at /data/gitnexus

# The official image already exposes port 4747 by default, 
# and runs `gitnexus serve --host 0.0.0.0 --port 4747` automatically.
